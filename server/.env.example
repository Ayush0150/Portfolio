# Express server port
PORT=5050

# Comma-separated allowed origins for CORS (your client URL)
CLIENT_URL=http://localhost:5173

# Email delivery via Resend (optional). Without it, the contact form falls
# back to opening the visitor's mail client (mailto).
RESEND_API_KEY=
CONTACT_EMAIL=raiaayushrai2005@gmail.com
